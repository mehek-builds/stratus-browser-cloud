{
  "name": "stratus-browser-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A clean-room browser-agent cloud with self-hosted and free managed modes.",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "NODE_ENV=test node --test --test-concurrency=1 test/*.test.js",
    "test:runner": "LITOS_POST_SUBMIT_SETTLE_MS=1500 NODE_ENV=test node test/managed-runner-replay.mjs",
    "test:choice-parity": "LITOS_POST_SUBMIT_SETTLE_MS=1500 NODE_ENV=test node test/choice-parity-replay.mjs",
    "test:security-code": "LITOS_POST_SUBMIT_SETTLE_MS=9000 NODE_ENV=test node test/security-code-replay.mjs",
    "test:required-confirmation": "LITOS_POST_SUBMIT_SETTLE_MS=1500 NODE_ENV=test node test/required-confirmation-replay.mjs",
    "test:e2e": "LITOS_POST_SUBMIT_SETTLE_MS=1500 NODE_ENV=test node test/e2e.mjs",
    "test:captcha-visibility": "LITOS_POST_SUBMIT_SETTLE_MS=1500 NODE_ENV=test node test/captcha-visibility-replay.mjs",
    "verify": "npm test && npm run test:runner && npm run test:choice-parity && npm run test:security-code && npm run test:required-confirmation && npm run test:captcha-visibility && npm run test:e2e",
    "dev:vercel": "vercel dev"
  },
  "dependencies": {
    "@vercel/sandbox": "^2.8.0",
    "jose": "^6.2.4",
    "playwright-core": "^1.54.1",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=22"
  }
}
